import Logo from "@/app/components/Logo";
import Nav from "@/app/components/Nav";

const Header = () => {
  return (
    <header>
      <Logo />
      <Nav />
    </header>
  );
};

export default Header;
